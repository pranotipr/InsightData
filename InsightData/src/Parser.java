public interface Parser {
	/**
	 * Implements the file parsing logic, and builds the output string.
	 * @param inputFile a string containing input file path
	 * @param outputFile a string containing output file path
	 * @author Pranoti
	 * 
	 */
	public void parse(String inputFile, String outputFile);
}